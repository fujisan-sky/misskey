--- packages/frontend/test/aiscript/api.test.ts	2026-07-02 10:19:31.614760320 +0900
+++ packages/frontend/test/aiscript/api.test.ts	2026-07-02 09:56:55.022926586 +0900
@@ -8,6 +8,7 @@
 import { errors, Interpreter, Parser, values } from '@syuilo/aiscript';
 import {
 	afterAll,
+	afterEach,
 	beforeAll,
 	beforeEach,
 	describe,
@@ -36,17 +37,6 @@
 	() => null
 );
 
-function errorWithPos<T extends errors.AiScriptError>(
-	error: T,
-	line: number,
-	column: number,
-): T {
-	const pos = { line, column };
-	error.pos = pos;
-	error.message = error.message + `\n  at <root> (Line ${pos.line}, Column ${pos.column})`;
-	return error;
-}
-
 vi.mock('@/i.js', () => {
 	return {
 		get $i() {
@@ -79,9 +69,8 @@
 	});
 
 	describe('readline', () => {
-		beforeEach(() => {
+		afterEach(() => {
 			vi.restoreAllMocks();
-			vi.clearAllMocks();
 		});
 
 		test.sequential('ok', async () => {
@@ -176,9 +165,8 @@
 	});
 
 	describe('dialog', () => {
-		beforeEach(() => {
+		afterEach(() => {
 			vi.restoreAllMocks();
-			vi.clearAllMocks();
 		});
 
 		test.sequential('ok', async () => {
@@ -216,9 +204,8 @@
 	});
 
 	describe('confirm', () => {
-		beforeEach(() => {
+		afterEach(() => {
 			vi.restoreAllMocks();
-			vi.clearAllMocks();
 		});
 
 		test.sequential('ok', async () => {
@@ -274,9 +261,8 @@
 	});
 
 	describe('api', () => {
-		beforeEach(() => {
+		afterEach(() => {
 			vi.restoreAllMocks();
-			vi.clearAllMocks();
 		});
 
 		test.sequential('successful', async () => {
@@ -330,7 +316,7 @@
 			await expect(() => exe(`
 				Mk:api('https://example.com/api/ping', {})
 			`)).rejects.toStrictEqual(
-				errorWithPos(new errors.AiScriptRuntimeError('invalid endpoint'), 2, 11),
+				new errors.AiScriptRuntimeError('invalid endpoint'),
 			);
 			expect(misskeyApiMock).not.toHaveBeenCalled();
 		});
@@ -339,7 +325,7 @@
 			await expect(() => exe(`
 				Mk:api('ping')
 			`)).rejects.toStrictEqual(
-				errorWithPos(new errors.AiScriptRuntimeError('expected param'), 2, 11),
+				new errors.AiScriptRuntimeError('expected param'),
 			);
 			expect(misskeyApiMock).not.toHaveBeenCalled();
 		});
@@ -350,7 +336,7 @@
 			miLocalStorage.removeItem('aiscript:widget:key');
 		});
 
-		beforeEach(() => {
+		afterEach(() => {
 			miLocalStorage.removeItem('aiscript:widget:key');
 		});
 
@@ -367,7 +353,7 @@
 			await expect(() => exe(`
 				Mk:save('key')
 			`)).rejects.toStrictEqual(
-				errorWithPos(new errors.AiScriptRuntimeError('Expect anything, but got nothing.'), 2, 12),
+				new errors.AiScriptRuntimeError('Expect anything, but got nothing.'),
 			);
 		});
 
