--- packages/frontend/src/widgets/WidgetUserList.vue	2026-07-02 10:19:31.613760323 +0900
+++ packages/frontend/src/widgets/WidgetUserList.vue	2026-07-02 09:56:55.022926586 +0900
@@ -28,7 +28,7 @@
 import * as Misskey from 'misskey-js';
 import { useWidgetPropsManager } from './widget.js';
 import type { WidgetComponentEmits, WidgetComponentExpose, WidgetComponentProps } from './widget.js';
-import type { FormWithDefault, GetFormResultType } from '@/utility/form.js';
+import type { GetFormResultType } from '@/utility/form.js';
 import MkContainer from '@/components/MkContainer.vue';
 import * as os from '@/os.js';
 import { misskeyApi } from '@/utility/misskey-api.js';
@@ -40,16 +40,15 @@
 
 const widgetPropsDef = {
 	showHeader: {
-		type: 'boolean',
-		label: i18n.ts._widgetOptions.showHeader,
+		type: 'boolean' as const,
 		default: true,
 	},
 	listId: {
-		type: 'string',
-		default: null as string | null,
+		type: 'string' as const,
+		default: null,
 		hidden: true,
 	},
-} satisfies FormWithDefault;
+};
 
 type WidgetProps = GetFormResultType<typeof widgetPropsDef>;
 
@@ -62,21 +61,21 @@
 	emit,
 );
 
-const list = ref<Misskey.entities.UserList | null>(null);
+const list = ref<Misskey.entities.UserList>();
 const users = ref<Misskey.entities.UserDetailed[]>([]);
 const fetching = ref(true);
 
 async function chooseList() {
 	const lists = await misskeyApi('users/lists/list');
-	const { canceled, result: listId } = await os.select({
+	const { canceled, result: list } = await os.select({
 		title: i18n.ts.selectList,
 		items: lists.map(x => ({
-			value: x.id, label: x.name,
+			value: x, text: x.name,
 		})),
 		default: widgetProps.listId,
 	});
-	if (canceled || listId == null) return;
-	const list = lists.find(x => x.id === listId)!;
+	if (canceled) return;
+
 	widgetProps.listId = list.id;
 	save();
 	fetch();
@@ -93,7 +92,7 @@
 	}).then(_list => {
 		list.value = _list;
 		misskeyApi('users/show', {
-			userIds: list.value.userIds ?? [],
+			userIds: list.value.userIds,
 		}).then(_users => {
 			users.value = _users;
 			fetching.value = false;
