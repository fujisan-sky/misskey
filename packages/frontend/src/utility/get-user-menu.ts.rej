--- packages/frontend/src/utility/get-user-menu.ts	2026-07-02 10:19:31.609760337 +0900
+++ packages/frontend/src/utility/get-user-menu.ts	2026-07-02 09:56:55.018926593 +0900
@@ -37,15 +37,15 @@
 			const { canceled, result: period } = await os.select({
 				title: i18n.ts.mutePeriod,
 				items: [{
-					value: 'indefinitely', label: i18n.ts.indefinitely,
+					value: 'indefinitely', text: i18n.ts.indefinitely,
 				}, {
-					value: 'tenMinutes', label: i18n.ts.tenMinutes,
+					value: 'tenMinutes', text: i18n.ts.tenMinutes,
 				}, {
-					value: 'oneHour', label: i18n.ts.oneHour,
+					value: 'oneHour', text: i18n.ts.oneHour,
 				}, {
-					value: 'oneDay', label: i18n.ts.oneDay,
+					value: 'oneDay', text: i18n.ts.oneDay,
 				}, {
-					value: 'oneWeek', label: i18n.ts.oneWeek,
+					value: 'oneWeek', text: i18n.ts.oneWeek,
 				}],
 				default: 'indefinitely',
 			});
@@ -132,7 +132,6 @@
 		const userDetailed = await misskeyApi('users/show', {
 			userId: user.id,
 		});
-
 		const { canceled, result } = await os.form(i18n.ts.editMemo, {
 			memo: {
 				type: 'string',
@@ -142,7 +141,6 @@
 				default: userDetailed.memo,
 			},
 		});
-
 		if (canceled) return;
 
 		os.apiWithDialog('users/update-memo', {
@@ -158,11 +156,7 @@
 			icon: 'ti ti-user-exclamation',
 			text: i18n.ts.moderation,
 			action: () => {
-				router.push('/admin/user/:userId', {
-					params: {
-						userId: user.id,
-					},
-				});
+				router.push(`/admin/user/${user.id}`);
 			},
 		}, { type: 'divider' });
 	}
@@ -215,32 +209,12 @@
 		});
 	}
 
-	if ($i && meId === user.id) {
-		menuItems.push({
-			icon: 'ti ti-qrcode',
-			text: i18n.ts.qr,
-			action: () => {
-				router.push('/qr');
-			},
-		});
-	}
-
 	if (notesSearchAvailable && (user.host == null || canSearchNonLocalNotes)) {
 		menuItems.push({
 			icon: 'ti ti-search',
 			text: i18n.ts.searchThisUsersNotes,
 			action: () => {
-				const query = {
-						username: user.username,
-					} as { username: string, host?: string };
-
-				if (user.host !== null) {
-					query.host = user.host;
-				}
-
-				router.push('/search', {
-					query
-				});
+				router.push(`/search?username=${encodeURIComponent(user.username)}${user.host != null ? '&host=' + encodeURIComponent(user.host) : ''}`);
 			},
 		});
 	}
@@ -304,6 +278,7 @@
 							caseSensitive: antenna.caseSensitive,
 							withReplies: antenna.withReplies,
 							withFile: antenna.withFile,
+							notify: antenna.notify,
 						});
 						antennasCache.delete();
 					},
@@ -327,15 +302,15 @@
 							const { canceled, result: period } = await os.select({
 								title: i18n.ts.period + ': ' + r.name,
 								items: [{
-									value: 'indefinitely', label: i18n.ts.indefinitely,
+									value: 'indefinitely', text: i18n.ts.indefinitely,
 								}, {
-									value: 'oneHour', label: i18n.ts.oneHour,
+									value: 'oneHour', text: i18n.ts.oneHour,
 								}, {
-									value: 'oneDay', label: i18n.ts.oneDay,
+									value: 'oneDay', text: i18n.ts.oneDay,
 								}, {
-									value: 'oneWeek', label: i18n.ts.oneWeek,
+									value: 'oneWeek', text: i18n.ts.oneWeek,
 								}, {
-									value: 'oneMonth', label: i18n.ts.oneMonth,
+									value: 'oneMonth', text: i18n.ts.oneMonth,
 								}],
 								default: 'indefinitely',
 							});
@@ -381,8 +356,8 @@
 		//}
 
 		menuItems.push({ type: 'divider' }, {
-			icon: 'ti ti-pencil-heart',
-			text: i18n.ts.createUserSpecifiedNote,
+			icon: 'ti ti-mail',
+			text: i18n.ts.sendMessage,
 			action: () => {
 				const canonical = user.host === null ? `@${user.username}` : `@${user.username}@${user.host}`;
 				os.post({ specified: user, initialText: `${canonical} ` });
