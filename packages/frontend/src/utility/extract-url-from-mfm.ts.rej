--- packages/frontend/src/utility/extract-url-from-mfm.ts	2026-07-02 10:19:31.608760341 +0900
+++ packages/frontend/src/utility/extract-url-from-mfm.ts	2026-07-02 09:56:55.018926593 +0900
@@ -13,7 +13,7 @@
 export function extractUrlFromMfm(nodes: mfm.MfmNode[], respectSilentFlag = true): string[] {
 	const urlNodes = mfm.extract(nodes, (node) => {
 		return (node.type === 'url') || (node.type === 'link' && (!respectSilentFlag || !node.props.silent));
-	}) as mfm.MfmUrl[];
+	});
 	const urls: string[] = unique(urlNodes.map(x => x.props.url));
 
 	return urls.reduce((array, url) => {
