--- packages/frontend/src/utility/chart-vline.ts	2026-07-02 10:19:31.608760341 +0900
+++ packages/frontend/src/utility/chart-vline.ts	2026-07-02 09:56:55.017926595 +0900
@@ -8,10 +8,9 @@
 export const chartVLine = (vLineColor: string) => ({
 	id: 'vLine',
 	beforeDraw(chart, args, options) {
-		const tooltip = chart.tooltip as any;
-		if (tooltip?._active?.length) {
+		if (chart.tooltip?._active?.length) {
 			const ctx = chart.ctx;
-			const xs = tooltip._active.map((a: any) => a.element.x) as number[];
+			const xs = chart.tooltip._active.map(a => a.element.x);
 			const x = xs.reduce((a, b) => a + b, 0) / xs.length;
 			const topY = chart.scales.y.top;
 			const bottomY = chart.scales.y.bottom;
