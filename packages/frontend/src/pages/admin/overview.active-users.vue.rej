--- packages/frontend/src/pages/admin/overview.active-users.vue	2026-07-02 10:19:31.598760376 +0900
+++ packages/frontend/src/pages/admin/overview.active-users.vue	2026-07-02 09:56:55.001926627 +0900
@@ -26,7 +26,7 @@
 
 const chartEl = useTemplateRef('chartEl');
 const now = new Date();
-let chartInstance: Chart | null = null;
+let chartInstance: Chart = null;
 const chartLimit = 7;
 const fetching = ref(true);
 
@@ -37,8 +37,6 @@
 		chartInstance.destroy();
 	}
 
-	if (chartEl.value == null) return;
-
 	const getDate = (ago: number) => {
 		const y = now.getFullYear();
 		const m = now.getMonth();
@@ -47,7 +45,7 @@
 		return new Date(y, m, d - ago);
 	};
 
-	const format = (arr: number[]) => {
+	const format = (arr) => {
 		return arr.map((v, i) => ({
 			x: getDate(i).getTime(),
 			y: v,
@@ -107,6 +105,7 @@
 					type: 'time',
 					offset: true,
 					time: {
+						stepSize: 1,
 						unit: 'day',
 						displayFormats: {
 							day: 'M/d',
@@ -150,9 +149,7 @@
 					},
 					external: externalTooltipHandler,
 				},
-				...({ // TSを黙らすため
-					gradient,
-				}),
+				gradient,
 			},
 		},
 		plugins: [chartVLine(vLineColor)],
