--- packages/frontend/src/pages/chat/room.vue	2026-07-02 10:19:31.600760369 +0900
+++ packages/frontend/src/pages/chat/room.vue	2026-07-02 09:56:55.004926621 +0900
@@ -197,7 +197,7 @@
 		connection.value.on('deleted', onDeleted);
 		connection.value.on('react', onReact);
 		connection.value.on('unreact', onUnreact);
-	} else if (props.roomId) {
+	} else {
 		const [rResult, mResult] = await Promise.allSettled([
 			misskeyApi('chat/rooms/show', { roomId: props.roomId }),
 			misskeyApi('chat/messages/room-timeline', { roomId: props.roomId, limit: LIMIT }),
@@ -391,7 +391,7 @@
 	router.push('/chat');
 }
 
-function showMenu(ev: PointerEvent) {
+function showMenu(ev: MouseEvent) {
 	const menuItems: MenuItem[] = [];
 
 	if (room.value) {
@@ -421,7 +421,7 @@
 
 const headerTabs = computed(() => room.value ? [{
 	key: 'chat',
-	title: i18n.ts._chat.messages,
+	title: i18n.ts.chat,
 	icon: 'ti ti-messages',
 }, {
 	key: 'members',
@@ -437,7 +437,7 @@
 	icon: 'ti ti-info-circle',
 }] : [{
 	key: 'chat',
-	title: i18n.ts._chat.messages,
+	title: i18n.ts.chat,
 	icon: 'ti ti-messages',
 }, {
 	key: 'search',
@@ -447,6 +447,7 @@
 
 const headerActions = computed<PageHeaderItem[]>(() => [{
 	icon: 'ti ti-dots',
+	text: '',
 	handler: showMenu,
 }]);
 
@@ -465,12 +466,12 @@
 			};
 		} else {
 			return {
-				title: i18n.ts.directMessage,
+				title: i18n.ts.chat,
 			};
 		}
 	} else {
 		return {
-			title: i18n.ts.directMessage,
+			title: i18n.ts.chat,
 		};
 	}
 }));
