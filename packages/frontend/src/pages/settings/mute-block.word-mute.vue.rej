--- packages/frontend/src/pages/settings/mute-block.word-mute.vue	2026-07-02 10:19:31.604760355 +0900
+++ packages/frontend/src/pages/settings/mute-block.word-mute.vue	2026-07-02 09:56:55.009926612 +0900
@@ -30,7 +30,7 @@
 	(ev: 'save', value: (string[] | string)[]): void;
 }>();
 
-const render = (mutedWords: (string | string[])[]) => mutedWords.map(x => {
+const render = (mutedWords) => mutedWords.map(x => {
 	if (Array.isArray(x)) {
 		return x.join(' ');
 	} else {
@@ -46,13 +46,13 @@
 });
 
 async function save() {
-	const parseMutes = (mutes: string) => {
+	const parseMutes = (mutes) => {
 		// split into lines, remove empty lines and unnecessary whitespace
-		let lines = mutes.trim().split('\n').map(line => line.trim()).filter(line => line !== '') as (string | string[])[];
+		let lines = mutes.trim().split('\n').map(line => line.trim()).filter(line => line !== '');
 
 		// check each line if it is a RegExp or not
 		for (let i = 0; i < lines.length; i++) {
-			const line = lines[i] as string;
+			const line = lines[i];
 			const regexp = line.match(/^\/(.+)\/(.*)$/);
 			if (regexp) {
 				// check that the RegExp is valid
