--- packages/frontend/src/composables/use-tooltip.ts	2026-07-02 10:19:31.595760387 +0900
+++ packages/frontend/src/composables/use-tooltip.ts	2026-07-02 09:56:54.996926637 +0900
@@ -22,7 +22,7 @@
 
 	let changeShowingState: (() => void) | null;
 
-	let autoHidingTimer: number | null = null;
+	let autoHidingTimer;
 
 	const open = () => {
 		close();
@@ -43,7 +43,7 @@
 				isHovering = false;
 				window.clearTimeout(timeoutId);
 				close();
-				if (autoHidingTimer != null) window.clearInterval(autoHidingTimer);
+				window.clearInterval(autoHidingTimer);
 			}
 		}, 1000);
 	};
@@ -66,7 +66,7 @@
 		if (!isHovering) return;
 		isHovering = false;
 		window.clearTimeout(timeoutId);
-		if (autoHidingTimer != null) window.clearInterval(autoHidingTimer);
+		window.clearInterval(autoHidingTimer);
 		close();
 	};
 
@@ -81,7 +81,7 @@
 		if (!isHovering) return;
 		isHovering = false;
 		window.clearTimeout(timeoutId);
-		if (autoHidingTimer != null) window.clearInterval(autoHidingTimer);
+		window.clearInterval(autoHidingTimer);
 		close();
 	};
 
