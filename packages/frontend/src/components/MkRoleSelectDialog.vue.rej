--- packages/frontend/src/components/MkRoleSelectDialog.vue	2026-07-02 10:19:31.590760405 +0900
+++ packages/frontend/src/components/MkRoleSelectDialog.vue	2026-07-02 09:56:54.988926653 +0900
@@ -55,9 +55,9 @@
 import MkLoading from '@/components/global/MkLoading.vue';
 
 const emit = defineEmits<{
-	(ev: 'done', value: Misskey.entities.Role[]): void;
-	(ev: 'close'): void;
-	(ev: 'closed'): void;
+	(ev: 'done', value: Misskey.entities.Role[]),
+	(ev: 'close'),
+	(ev: 'closed'),
 }>();
 
 const props = withDefaults(defineProps<{
@@ -102,12 +102,14 @@
 	const items = roles.value
 		.filter(r => r.isPublic)
 		.filter(r => !selectedRoleIds.value.includes(r.id))
-		.map(r => ({ label: r.name, value: r.id }));
+		.map(r => ({ text: r.name, value: r }));
 
-	const { canceled, result: roleId } = await os.select({ items });
-	if (canceled || roleId == null) return;
+	const { canceled, result: role } = await os.select({ items });
+	if (canceled) {
+		return;
+	}
 
-	selectedRoleIds.value.push(roleId);
+	selectedRoleIds.value.push(role.id);
 }
 
 async function removeRole(roleId: string) {
