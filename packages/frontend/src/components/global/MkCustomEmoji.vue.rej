--- packages/frontend/src/components/global/MkCustomEmoji.vue	2026-07-02 10:19:31.593760394 +0900
+++ packages/frontend/src/components/global/MkCustomEmoji.vue	2026-07-02 09:56:54.993926643 +0900
@@ -50,7 +50,6 @@
 import { prefer } from '@/preferences.js';
 import { DI } from '@/di.js';
 import { makeEmojiMuteKey, mute as muteEmoji, unmute as unmuteEmoji, checkMuted as checkEmojiMuted } from '@/utility/emoji-mute';
-import { addToEmojiPalette } from '@/utility/emoji-palette.js';
 
 const props = defineProps<{
 	name: string;
@@ -103,7 +102,7 @@
 const alt = computed(() => `:${customEmojiName.value}:`);
 const errored = ref(url.value == null);
 
-function onClick(ev: PointerEvent) {
+function onClick(ev: MouseEvent) {
 	if (props.menu) {
 		const menuItems: MenuItem[] = [];
 
@@ -168,20 +167,8 @@
 			});
 		}
 
-		if (isLocal.value) {
-			menuItems.push({
-				text: i18n.ts.addToEmojiPalette,
-				icon: 'ti ti-palette',
-				action: () => {
-					addToEmojiPalette(`:${props.name}:`);
-				},
-			});
-		}
-
 		if (($i?.isModerator ?? $i?.isAdmin) && isLocal.value) {
 			menuItems.push({
-				type: 'divider',
-			}, {
 				text: i18n.ts.edit,
 				icon: 'ti ti-pencil',
 				action: async () => {
