--- packages/frontend/src/components/global/StackingRouterView.vue	2026-07-02 10:19:31.594760390 +0900
+++ packages/frontend/src/components/global/StackingRouterView.vue	2026-07-02 09:56:54.995926639 +0900
@@ -74,10 +74,9 @@
 }
 
 function back() {
-	if (tabs.value.length <= 1) return; // transitionの関係でタブが1つの状態でbackが呼ばれることがある
 	const prev = tabs.value[tabs.value.length - 2];
 	tabs.value = [...tabs.value.slice(0, tabs.value.length - 1)];
-	router?.replaceByPath(prev.fullPath);
+	router.replace(prev.fullPath);
 }
 
 router.useListener('change', ({ resolved }) => {
@@ -88,7 +87,7 @@
 	const fullPath = router.getCurrentFullPath();
 
 	if (tabs.value.some(tab => tab.routePath === routePath && deepEqual(resolved.props, tab.props))) {
-		const newTabs = [] as typeof tabs.value;
+		const newTabs = [];
 		for (const tab of tabs.value) {
 			newTabs.push(tab);
 
@@ -171,6 +170,12 @@
 			width: 100%;
 			height: 100%;
 		}
+
+		.tabContent {
+			position: relative;
+			width: 100%;
+			height: 100%;
+		}
 	}
 
 	&:not(:first-child) {
@@ -203,17 +208,13 @@
 
 		.tabContent {
 			flex: 1;
+			width: 100%;
+			height: 100%;
+			background: var(--MI_THEME-bg);
 		}
 	}
 }
 
-.tabContent {
-	position: relative;
-	width: 100%;
-	height: 100%;
-	background: var(--MI_THEME-bg);
-}
-
 .tabMenu {
 	position: relative;
 	margin-left: auto;
