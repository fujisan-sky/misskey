--- packages/frontend/src/components/MkTutorialDialog.Note.vue	2026-07-02 10:19:31.591760401 +0900
+++ packages/frontend/src/components/MkTutorialDialog.Note.vue	2026-07-02 09:56:54.991926647 +0900
@@ -29,7 +29,6 @@
 import { globalEvents } from '@/events.js';
 import { $i } from '@/i.js';
 import MkNote from '@/components/MkNote.vue';
-import { genId } from '@/utility/id.js';
 
 const props = defineProps<{
 	phase: 'aboutNote' | 'howToReact';
@@ -74,7 +73,7 @@
 });
 const onceReacted = ref<boolean>(false);
 
-function addReaction(emoji: string) {
+function addReaction(emoji) {
 	onceReacted.value = true;
 	emit('reacted');
 	doNotification(emoji);
@@ -84,7 +83,7 @@
 	if (!$i || !emoji) return;
 
 	const notification: Misskey.entities.Notification = {
-		id: genId(),
+		id: Math.random().toString(),
 		createdAt: new Date().toUTCString(),
 		type: 'reaction',
 		reaction: emoji,
@@ -96,7 +95,7 @@
 	globalEvents.emit('clientNotification', notification);
 }
 
-function removeReaction(emoji: string) {
+function removeReaction(emoji) {
 	delete exampleNote.reactions[emoji];
 	exampleNote.myReaction = undefined;
 }
