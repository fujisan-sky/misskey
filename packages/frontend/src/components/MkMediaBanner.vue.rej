--- packages/frontend/src/components/MkMediaBanner.vue	2026-07-02 10:19:31.587760415 +0900
+++ packages/frontend/src/components/MkMediaBanner.vue	2026-07-02 09:56:54.985926659 +0900
@@ -6,7 +6,7 @@
 <template>
 <div :class="$style.root">
 	<MkMediaAudio v-if="media.type.startsWith('audio') && media.type !== 'audio/midi'" :audio="media"/>
-	<div v-else-if="hide" :class="$style.sensitive" @click="reveal">
+	<div v-else-if="media.isSensitive && hide" :class="$style.sensitive" @click="show">
 		<span style="font-size: 1.6em;"><i class="ti ti-alert-triangle"></i></span>
 		<b>{{ i18n.ts.sensitive }}</b>
 		<span>{{ i18n.ts.clickToShow }}</span>
@@ -27,18 +27,23 @@
 import { ref } from 'vue';
 import * as Misskey from 'misskey-js';
 import { i18n } from '@/i18n.js';
+import * as os from '@/os.js';
 import MkMediaAudio from '@/components/MkMediaAudio.vue';
-import { shouldHideFileByDefault, canRevealFile } from '@/utility/sensitive-file.js';
+import { prefer } from '@/preferences.js';
 
 const props = defineProps<{
 	media: Misskey.entities.DriveFile;
 }>();
 
-const hide = ref(shouldHideFileByDefault(props.media));
+const hide = ref(true);
 
-async function reveal() {
-	if (!(await canRevealFile(props.media))) {
-		return;
+async function show() {
+	if (props.media.isSensitive && prefer.s.confirmWhenRevealingSensitiveMedia) {
+		const { canceled } = await os.confirm({
+			type: 'question',
+			text: i18n.ts.sensitiveMediaRevealConfirm,
+		});
+		if (canceled) return;
 	}
 
 	hide.value = false;
