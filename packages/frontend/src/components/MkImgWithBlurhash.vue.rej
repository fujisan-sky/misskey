--- packages/frontend/src/components/MkImgWithBlurhash.vue	2026-07-02 10:19:31.587760415 +0900
+++ packages/frontend/src/components/MkImgWithBlurhash.vue	2026-07-02 09:56:54.984926661 +0900
@@ -25,7 +25,7 @@
 			draggable="false"
 			tabindex="-1"
 			style="-webkit-user-drag: none;"
-		></canvas>
+		/>
 		<img
 			v-show="!hide"
 			key="img"
@@ -52,20 +52,15 @@
 import { WorkerMultiDispatch } from '@@/js/worker-multi-dispatch.js';
 import { extractAvgColorFromBlurhash } from '@@/js/extract-avg-color-from-blurhash.js';
 
-// テスト環境で Web Worker インスタンスは作成できない
-// eslint-disable-next-line @typescript-eslint/ban-ts-comment
-// @ts-expect-error
-const isTest = (import.meta.env.MODE === 'test' || window.Cypress != null);
-
 const canvasPromise = new Promise<WorkerMultiDispatch | HTMLCanvasElement>(resolve => {
-	if (isTest) {
+	// テスト環境で Web Worker インスタンスは作成できない
+	if (import.meta.env.MODE === 'test') {
 		const canvas = window.document.createElement('canvas');
 		canvas.width = 64;
 		canvas.height = 64;
 		resolve(canvas);
 		return;
 	}
-
 	const testWorker = new TestWebGL2();
 	testWorker.addEventListener('message', event => {
 		if (event.data.result) {
@@ -87,7 +82,7 @@
 
 <script lang="ts" setup>
 import { computed, nextTick, onMounted, onUnmounted, useTemplateRef, watch, ref } from 'vue';
-import { genId } from '@/utility/id.js';
+import { v4 as uuid } from 'uuid';
 import { render } from 'buraha';
 import { prefer } from '@/preferences.js';
 
@@ -122,7 +117,7 @@
 	onlyAvgColor: false,
 });
 
-const viewId = genId();
+const viewId = uuid();
 const canvas = useTemplateRef('canvas');
 const root = useTemplateRef('root');
 const img = useTemplateRef('img');
@@ -194,7 +189,7 @@
 }
 
 async function draw() {
-	if (isTest && props.hash == null) return;
+	if (import.meta.env.MODE === 'test' && props.hash == null) return;
 
 	drawAvg();
 
