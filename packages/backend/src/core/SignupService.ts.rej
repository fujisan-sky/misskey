--- packages/backend/src/core/SignupService.ts	2026-07-02 10:19:31.531760613 +0900
+++ packages/backend/src/core/SignupService.ts	2026-07-02 09:56:54.895926838 +0900
@@ -93,11 +93,6 @@
 			if (isPreserved) {
 				throw new Error('USED_USERNAME');
 			}
-
-			const hasProhibitedWords = this.utilityService.isKeyWordIncluded(username.toLowerCase(), this.meta.prohibitedWordsForNameOfUser);
-			if (hasProhibitedWords) {
-				throw new Error('USED_USERNAME');
-			}
 		}
 
 		const keyPair = await new Promise<string[]>((res, rej) =>
@@ -164,3 +159,4 @@
 		return { account, secret };
 	}
 }
+
