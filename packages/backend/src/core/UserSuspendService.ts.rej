--- packages/backend/src/core/UserSuspendService.ts	2026-07-02 10:19:31.531760613 +0900
+++ packages/backend/src/core/UserSuspendService.ts	2026-07-02 09:56:54.896926836 +0900
@@ -49,8 +49,8 @@
 		});
 
 		(async () => {
-			await this.postSuspend(user).catch(_ => {});
-			await this.unFollowAll(user).catch(_ => {});
+			await this.postSuspend(user).catch(e => {});
+			await this.unFollowAll(user).catch(e => {});
 		})();
 	}
 
@@ -67,7 +67,7 @@
 		});
 
 		(async () => {
-			await this.postUnsuspend(user).catch(_ => {});
+			await this.postUnsuspend(user).catch(e => {});
 		})();
 	}
 
@@ -93,10 +93,7 @@
 					{ followerSharedInbox: Not(IsNull()) },
 					{ followeeSharedInbox: Not(IsNull()) },
 				],
-				select: {
-					followerSharedInbox: true,
-					followeeSharedInbox: true,
-				},
+				select: ['followerSharedInbox', 'followeeSharedInbox'],
 			});
 
 			const inboxes = followings.map(x => x.followerSharedInbox ?? x.followeeSharedInbox);
@@ -126,10 +123,7 @@
 					{ followerSharedInbox: Not(IsNull()) },
 					{ followeeSharedInbox: Not(IsNull()) },
 				],
-				select: {
-					followerSharedInbox: true,
-					followeeSharedInbox: true,
-				},
+				select: ['followerSharedInbox', 'followeeSharedInbox'],
 			});
 
 			const inboxes = followings.map(x => x.followerSharedInbox ?? x.followeeSharedInbox);
