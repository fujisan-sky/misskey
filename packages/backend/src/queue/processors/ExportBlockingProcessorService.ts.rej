--- packages/backend/src/queue/processors/ExportBlockingProcessorService.ts	2026-07-02 10:19:31.542760574 +0900
+++ packages/backend/src/queue/processors/ExportBlockingProcessorService.ts	2026-07-02 09:56:54.909926810 +0900
@@ -58,10 +58,6 @@
 			let exportedCount = 0;
 			let cursor: MiBlocking['id'] | null = null;
 
-			const total = await this.blockingsRepository.countBy({
-				blockerId: user.id,
-			});
-
 			while (true) {
 				const blockings = await this.blockingsRepository.find({
 					where: {
@@ -101,7 +97,11 @@
 					exportedCount++;
 				}
 
-				job.updateProgress(exportedCount / total * 100);
+				const total = await this.blockingsRepository.countBy({
+					blockerId: user.id,
+				});
+
+				job.updateProgress(exportedCount / total);
 			}
 
 			stream.end();
