--- packages/backend/src/queue/processors/DeleteDriveFilesProcessorService.ts	2026-07-02 10:19:31.542760574 +0900
+++ packages/backend/src/queue/processors/DeleteDriveFilesProcessorService.ts	2026-07-02 09:56:54.909926810 +0900
@@ -43,10 +43,6 @@
 		let deletedCount = 0;
 		let cursor: MiDriveFile['id'] | null = null;
 
-		const total = await this.driveFilesRepository.countBy({
-			userId: user.id,
-		});
-
 		while (true) {
 			const files = await this.driveFilesRepository.find({
 				where: {
@@ -71,7 +67,11 @@
 				deletedCount++;
 			}
 
-			job.updateProgress(deletedCount / total * 100);
+			const total = await this.driveFilesRepository.countBy({
+				userId: user.id,
+			});
+
+			job.updateProgress(deletedCount / total);
 		}
 
 		this.logger.succ(`All drive files (${deletedCount}) of ${user.id} has been deleted.`);
