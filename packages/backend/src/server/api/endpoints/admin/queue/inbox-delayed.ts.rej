--- packages/backend/src/server/api/endpoints/admin/queue/inbox-delayed.ts	2026-07-02 10:19:31.546760560 +0900
+++ packages/backend/src/server/api/endpoints/admin/queue/inbox-delayed.ts	2026-07-02 09:56:54.914926800 +0900
@@ -52,14 +52,18 @@
 		super(meta, paramDef, async (ps, me) => {
 			const jobs = await this.inboxQueue.getJobs(['delayed']);
 
-			const counts = new Map<string, number>();
+			const res = [] as [string, number][];
 
 			for (const job of jobs) {
 				const host = new URL(job.data.signature.keyId).host;
-				counts.set(host, (counts.get(host) ?? 0) + 1);
+				if (res.find(x => x[0] === host)) {
+					res.find(x => x[0] === host)![1]++;
+				} else {
+					res.push([host, 1]);
+				}
 			}
 
-			const res = [...counts.entries()].sort((a, b) => b[1] - a[1]);
+			res.sort((a, b) => b[1] - a[1]);
 
 			return res;
 		});
